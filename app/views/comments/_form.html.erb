<%= form_for([@review,@review.comments.build]) do |f| %>
  <p>
    <%= f.label :name %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <br>
  <p>
    <%= f.submit %>
  </p>
<% end %>
